void main() {
  // int num = 31;
  // if (num % 5 == 0 || num % 7 == 0) {
  //   if (num % 7 == 0 && num % 7 == 0) {
  //     print("Divisible by both");
  //   }
  //   else if (num % 5 == 0) {
  //     print("Divisible by 5");
  //   }
  //   else if (num % 7 == 0) {
  //     print("Divisible by 7");
  //   }
  // } else {
  //   print("Num not divisible by both 5 and 7");
  // }
  // List<int> numbers = [1, 2, 3];
  // print(numbers[0]);
  List<String> stdNames = ["Saad", "Babita", "jethalal"];
  // print(stdNames[0]);

  stdNames.sort();

  // stdNames.replaceRange(0, 3, ["Mohsin", "Iyer", "Daya"]);
  print(stdNames);
  // int index = stdNames.length - 2;
  // String secondLastEle = stdNames[index];
  // print(secondLastEle);
  // int length = stdNames.length;
  // String firstEle = stdNames.first;
  // String lastEle = stdNames.last;
  // String element = stdNames.elementAt(1);

  // print(length);
  // print(firstEle);
  // print(lastEle);
  // print(element);
}
